mkdir q03
cd q03
git init
echo "mode=normal" > config.txt
git add config.txt
git commit -m "main: initial config mode=normal"
git checkout -b feature-a
echo "mode=safe" > config.txt
git add config.txt
git commit -m "feature‑a: set mode=safe"
git checkout main
git checkout -b feature-b
echo "mode=fast" > config.txtmkdir q03
cd q03
git init
echo "mode=normal" > config.txt
git add config.txt
git commit -m "main: initial config mode=normal"
git checkout -b feature-a
echo "mode=safe" > config.txt
git add config.txt
git commit -m "feature‑a: set mode=safe"
git checkout main
git checkout -b feature-b
echo "mode=fast" > config.txt
git add config.txt
git commit -m "feature‑b: set mode=fast"
git checkout main
git merge feature-a
git merge feature-b
修改文件内容：
mode=safe
note=reviewed
git add config.txt
git commit -m "merge feature‑b: resolve conflict keep safe, add note=reviewed"
git log --all --graph --decorate --oneline

git add config.txt
git commit -m "feature‑b: set mode=fast"
git checkout main
git merge feature-a
git merge feature-b
cat > config.txt << EOF
mode=safe
note=reviewed
EOF
git add config.txt
git commit -m "merge feature‑b: resolve conflict keep safe, add note=reviewed"
git log --all --graph --decorate --oneline

